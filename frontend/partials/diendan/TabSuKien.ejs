<!-- Su kien Tab -->
<section id="tab-events" class="tab-pane">
    <div class="content-header">
        <h2>Sự kiện sắp tới</h2>
        <button class="btn-primary" id="btnCreateEvent" style="display: none;"
            onclick="openModal('createEventModal')">
            <i class="fas fa-plus"></i> Tạo sự kiện
        </button>
    </div>
    <div id="eventsList" class="events-grid">
        <!-- Sự kiện sẽ được tải ở đây -->
    </div>
</section>
